# 🏛️ 에이전트 시스템 헌법 (The Constitution)
**우선순위: CRITICAL — 시스템 전체 적용**

## 제0조: 폴더 완전 독립 격리 선언
> **모든 앱 폴더는 완전히 독립된 별개의 프로젝트다.**
> 폴더 간 교차 참조 및 연동은 절대 불가하며, 구체적인 행동 지침은 반드시 `directives/00_FOLDER_ISOLATION_RULE.md`를 따른다.

## 제1조: 출력 최적화 및 무음 실행 (속도/비용 최적화)
**1줄 완료 보고:** 작업 완료 시 오직 규격화된 1줄짜리 상태 보고(예: `✅ [PM] 기획안 .tmp/plan.md 저장 완료`)만 출력한다.

## 제2조: 컨텍스트 릴레이 및 압축 (정확도 향상)
1. **맥락 초기화:** 이전 에이전트의 불필요한 대화 기록은 다음 단계로 넘기지 않는다. 오직 확정된 최종 산출물(`.tmp/` 내 파일)만 전달하여 컨텍스트 부패를 막는다.
2. **데이터 압축:** 모든 산출물은 서술형 문장을 배제하고, JSON 스키마 또는 핵심 마크다운 형태로만 규격화하여 토큰을 최소화한다.

## 제3조: 도구 접근 격리 (확장성 확보)
각 에이전트는 자신에게 부여된 특정 도구(예: 기획자는 NotebookLM, 디자이너는 Stitch)만 로드하고 사용한다. 불필요한 도구를 전면 로드하여 성능을 저하시키지 않는다.

## 제4조: 3계층 아키텍처 및 파일 관리 규칙
- **1단계 (지시):** `directives/` - 프로젝트별 개별 지침서. 작업 전 필수 열람.
- **2단계 (조율):** AI 에이전트 - 의도 파악, 도구 호출, 에러 자가 치유(Self-Annealing) 수행.
- **3단계 (실행):** `execution/` - 파이썬 스크립트 기반의 결정적 결과 도출. 새 코드 작성 전 기존 스크립트를 우선 확인.
- **파일 보관:** 원본은 `resources/`, 임시 생성물은 `.tmp/`에 보관. 최종 결과물은 반드시 클라우드화한다.

## 제5조: 비즈니스 우선주의
01_PM 단계 완료 후 반드시 대표님의 명시적 승인(Go)을 받아야 진행 가능하다. 모든 보고는 전문 용어를 배제하고 대표님이 직관적으로 가치를 판단할 수 있게 구성한다.